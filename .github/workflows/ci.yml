name: Incident Management Platform CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: sqlite:///./test.db

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest -v

      - name: Docker Login
        uses: docker/login-action@v3
        with: 
          username: ${{ secrets.DOCKER_USERNAME}}
          password: ${{ secrets.DOCKER_PASSWORD}}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME}}/incidnet-management-platform:latest .
      
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME}}/incidnet-management-platform:latest

# CD disabled temporarily
# Reason:
# GitHub hosted runners cannot access local Docker Desktop Kubernetes cluster.
# Re-enable after moving Kubernetes to cloud.

 #     - name: Configure kubectl
 #       run: |
 #         mkdir -p ~/.kube 
 #         cat <<EOF > ~/.kube/config
 #         ${{ secrets.KUBE_CONFIG }}
 #         EOF

 #     - name: Deploy to Kubernetes
 #       run: |
 #         kubectl set image deployment/incident-api \
 #         incident-api=kappadaniel/incidnet-management-platform:latest

 #         kubectl rollout status deployment/incident-api